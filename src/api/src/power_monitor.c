#include <stdlib.h>
#include <stdio.h>
#include <string.h>
#include <sys/types.h>
#include <unistd.h>
#include <time.h>
#include "power_monitor.h"
#include "mf_api.h"

void power_monitor(int pid, char *DataPath, long sampling_interval)
{
	/*TOD : if it is necessary to measure power for each process, How it can be done? */
	exit(0);
}